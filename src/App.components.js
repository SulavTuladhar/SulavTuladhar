import { AppRouting } from "./AppRouting"

export const  App = (args) =>{

  return(
      <div style={{backgroundColor: '#436ba1', height:'100vh'}}>
      <AppRouting />
    </div>
  )
}
export default App;